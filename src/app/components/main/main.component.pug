#wrapper
  #main
    app-menu
    router-outlet
app-footer

#back-top(*ngIf="showScrollTop", (click)="scrollTo([0, 0, 'smooth'])")
  span.icon.icon-play

app-notificatione-dialog(*ngIf="notificationModal", [notification]="notificationText" , (result)="notificationModal = false")