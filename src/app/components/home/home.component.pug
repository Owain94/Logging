app-header([headerHeading]="['Home', '']")

.section
  .row
    .large-12.columns
      p(*ngIf="(settings | async)?.name; else no_settings") Hi, {{ (settings | async)?.name }} you're currently working from {{ (settings | async)?.location }} with the reason {{ (settings | async)?.invpre }}
      ng-template(#no_settings="")
        p No settings found, 
          a(routerLink="/settings") click here.
