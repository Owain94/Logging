p Delete log item
p([innerText]='"Are you sure you want to delete the log item?"')
button.button(md-raised-button, (click)='dialogRef.close(true)') Yes
button(md-button, (click)='dialogRef.close(false)') No